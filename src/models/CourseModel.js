export default class CourseModel {
	constructor(title, description, duration, authors, creationDate) {
		this.title = title;
		this.description = description;
		this.duration = duration;
		this.authors = authors;
		this.creationDate = creationDate;
	}
}
